# Copy this file to `.env` and fill in a value before starting locally.
# The office runner rolls AUTH_TOKEN per run automatically (RUN.json:
# "generate": "hex"); a human starting the product by hand sets it here.
AUTH_TOKEN=
